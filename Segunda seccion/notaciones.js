//notacion
//  ; final de una linea
const b = 4;
    //se puede utilizar sin ; aunque por recomendacion utilizar el ;

// . -> se utiliza para acceder a los atributos de los objetos
    // movil.anchura

// [] -> los corchetes se utilizan para las listas, para definirlos o acceder a los valores 
const ar = [1,2,3];
ar[1];
//() -> para las funciones
    function suma(a,b){
        return a + b;        
    }
// {} -> llaves para los objetos, funciones y estructutras nde control 
const movil2 = {
    anchura : 5,
    altura:10
}

